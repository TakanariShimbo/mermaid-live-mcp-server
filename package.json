{
  "name": "mermaid-live-mcp-server",
  "version": "0.1.0",
  "description": "A Model Context Protocol server for Mermaid Live diagram generation",
  "type": "module",
  "bin": {
    "mermaid-live-mcp-server": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "mermaid",
    "diagram",
    "visualization",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pako": "^2.0.3",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}